# Hospitality AI OS — AI Concierge

Read [docs/README.md](docs/README.md) before writing any code. It indexes 17 documents — PRD through the landing page plan — that already answer almost every design question this project has. **Never invent architecture, a data model, an API shape, or a behavior rule that already exists in `docs/`.** If something genuinely isn't decided yet, say so explicitly rather than guessing — and check [docs/README.md](docs/README.md)'s "Status" section first. The one thing that section used to name as open — the UI Design System choice ([08a–d](docs/08-ui-design-system.md)) — is now resolved: Option A, carrying Bellevue's real shipped materials (findings-log.md #25/#26); see Sprint 5 below.

## Current status (update this section as sprints close)

**Sprints 0 through 4 are complete; Sprint 5 tickets 1–5 are complete, with only the optional Option D behavior layer remaining.** Sprint 0: monorepo scaffolded, Prisma schema applied against a live Supabase database, RLS policies + restricted `app_role` runtime role applied and adversarially tested (PASS — see [DB §9](docs/07-database-design.md)), all credentials live. Sprint 1: core chat pipeline (`bootstrap` + `chat/message`, classifier → retrieval → generation → SSE) and the admin login shell (Supabase Auth), both verified live end to end with real streamed, grounded answers. Sprint 2: the full knowledge ingestion pipeline (parse → extract → chunk → tag → embed → validate) plus the admin HTTP surface and upload screen (`/admin/knowledge` — multipart upload, URL-sync, live per-stage status polling, chunk preview), verified live in-browser. Sprint 3: the full behavior spec — entity CRUD + relationship-bundle preview, the `card`/`lead_prompt`/`escalation`/`cta` SSE events wired into the live chat pipeline, every domain/persona prompt module composed onto `base.md`, and the full Golden Set (G-00–G-18) run by hand against the real pipeline (see [the run log](docs/sprint-3-golden-set-run.md)) — the guest experience described in the PRD now works end to end on the bare unstyled page. Every finding that run surfaced is fixed (see [Findings Log](docs/findings-log.md)). Work off [Sprint Backlog](docs/14-sprint-backlog.md) for exact, dated per-ticket detail — it's the record of what's actually been verified vs. deliberately deferred, not just a checklist to read once.

**Sprint 4 (Admin Portal Completion) is complete — all eight tickets done and merged.** Per its own Definition of Done in the [Sprint Backlog](docs/14-sprint-backlog.md)'s Sprint 4 section: no new backend concepts, only surfaces over data Sprints 1–3 already produce (plus the one exception ticket 8 itself needed — see below). Dashboard KPI tiles (`DailyMetric` rollups, API §3.6, [PR #20](https://github.com/salmannausher/Hospitality-OS-/pull/20)), conversation list + thread view + QA scoring (API §3.4, [PR #21](https://github.com/salmannausher/Hospitality-OS-/pull/21)), leads inbox incl. manual entry (API §3.4, [PR #22](https://github.com/salmannausher/Hospitality-OS-/pull/22)), Brand Settings with live preview + WCAG contrast validation (API §3.5, UI Design System §10, [PR #23](https://github.com/salmannausher/Hospitality-OS-/pull/23)), the Relationship Bundle builder with live guest-card preview (UX §10, [PR #25](https://github.com/salmannausher/Hospitality-OS-/pull/25) — purely a frontend build, Sprint 3 already shipped the full API), Analytics incl. the Missing Information panel (API §3.6, UX §12, [PR #26](https://github.com/salmannausher/Hospitality-OS-/pull/26)), Notifications wired to real events (new lead, escalation, ingestion failure — API §3.7, [PR #28](https://github.com/salmannausher/Hospitality-OS-/pull/28)), and `/session` + hotel CRUD + role-gating verified per role (API §3.1, [PR #29](https://github.com/salmannausher/Hospitality-OS-/pull/29)) — all live-verified in-browser plus a `verify-*.mjs` script apiece (except the bundle builder, which needed none — no backend code changed). Each surfaced at least one real doc/schema gap along the way (findings-log.md #12–#24): `DailyMetric` had no writer anywhere, `Message.escalationTriggered`/`leadCaptureTriggered` were never set, `Lead` had no `source` column despite the API spec assuming one, the WCAG contrast rule never specified which color/threshold, entity re-ingestion silently created duplicate rows, the Analytics mockup named topics finer-grained than the classifier can actually produce, `Notification` had no fan-out mechanism for its own documented Agency/Super Admin access model, and `admin_hotels_for_user()` never actually resolved a hotel for the one role its own doc comment says should get org-wide access — see the [Sprint Backlog](docs/14-sprint-backlog.md)'s per-ticket entries for exactly how each was resolved. Ticket 8 also closed out every "role-gating deliberately not enforced yet" note left across tickets 2–4: `VIEWER` can read but never mutate (enforced once, in `HotelScopeGuard`, covering every admin route automatically) and `MARKETING` can't reassign a lead — the only two rules any doc actually specifies; nothing further was invented for `RESERVATIONS` or the other roles.

**Sprint 5 (Visual Design System + Demo Property) tickets 1–5 are complete and merged — [PR #31](https://github.com/salmannausher/Hospitality-OS-/pull/31), [PR #32](https://github.com/salmannausher/Hospitality-OS-/pull/32).** Ticket 1 (the A/B/C/D decision) resolved to Option A's architecture carrying Bellevue's real shipped materials (brass, Cormorant Garamond + Work Sans), not a generic example palette — findings-log.md #25/#26. Ticket 2 built `packages/ui`'s guest-widget component library (tokens + `WidgetShell`/`Launcher`/`ConciergeMessage`/`GuestMessage`/`RecommendationCard`/`SuggestedChip`/`YesNoConfirm`/`EscalationPanel`/`TypingIndicator`) against the real chat pipeline. Ticket 4 built `packages/widget-embed` — a real `esbuild`-bundled, self-mounting `<script>` IIFE, not a React import. Ticket 5 wired that script onto `apps/demo-bellevue` for real, deleting the old visual-only `ConciergeWidget.tsx` stub. Getting this live-verified end to end surfaced a long, connected chain of real bugs — Sprint-1 stopgap seed content never retired after real ingestion landed (wrong prices, wrong facts), real content chunks that had never actually been retrievable at all (empty `domainTags`, then all 7 documents stuck in `NEEDS_REVIEW` since an earlier AI-Gateway-blocked ingestion run), a structural gap between the confidence formula and deliberately-atomic table chunks, a duplicate-entity bug reaching further than previously known, Prisma transaction timeouts under real Supabase pooler latency, and — after all of that — a widget panel geometry bug plus a stale-bundle caching gotcha. Every one is root-caused, fixed, and logged: [findings-log.md #27–#35](docs/findings-log.md). Ticket 3 (the demo site itself) and ticket 6 (Option D's optional behavior layer) — see their own line/note below. **The AI Gateway billing blocker is resolved** (the user purchased real credit, not just a card on file) — classifier and generation calls both work live now; Voyage's billing blocker was already resolved earlier.

**Post-Sprint-5 hardening pass (2026-08-03): an external code review (codex) surfaced 15 confirmed defects; the highest-priority ones are fixed and verified live.** Fixed: the chat widget's multi-field lead-capture flow silently stopped after the first field, discarding the server's `nextField` contract (both `packages/widget-embed` and the `apps/web` harness — findings-log.md #36); guest `conversationId`s weren't bound to the session that created them, letting one guest's conversation be hijacked by anyone who learned its id (`ChatService.openTurn` — #37); `MARKETING`/`RESERVATIONS` could mutate brand settings and trigger knowledge ingestion despite API §1 reserving both for `HOTEL_ADMIN+` (`HotelScopeGuard`'s new `@RequireRole` decorator — #38). Also built the durable production adapters Sprint 2 always deferred: `BullMqIngestionQueue` (real Upstash Redis, with the consumer side a bounded Vercel-Cron-driven `runTick()` sweep rather than a persistent `Worker` — this app has no long-running process for one to live in, a real gap the "swap is a one-module change" framing had glossed over, #40) and `SupabaseDocumentStorage` (plain-`fetch`-against-REST, matching `SupabaseAuthService`'s existing convention — #41); `KnowledgeModule` now selects these from env presence and fails fast in production if either is missing, rather than silently running on the old non-durable dev adapters. **#39 (public chat endpoint had no rate limiting, origin allowlist, or per-request size caps) is now fixed (2026-08-04)** — see findings-log.md #39 for the full build (Redis-backed limiter reusing the existing Upstash/BullMQ connection, a new `WidgetKey.allowedOrigins` column + fail-open-until-configured per-key origin check, and message/sessionId length caps), live-verified against the real running api and `wk_demo_bellevue`. The review's remaining Medium/Low findings (SSRF hardening on URL ingestion, consent-before-write on lead PII, lead-submission idempotency races, partial-stream persistence, unbounded analytics queries, widget accessibility gaps) are still untouched. "Missing audit logging" now has its own entry — findings-log.md #48 — alongside the three other admin-nav-item gaps (Prompt Settings/Integrations/Billing), scoped into Sprint 7 (Sprint Backlog).

**Sprint 6 (QA Pass, Pilot & Demo Prep) is functionally complete** — every Definition-of-Done item in the [Sprint Backlog](docs/14-sprint-backlog.md) is done except the meeting itself. The full Playbook (all 64 scenarios, G-00 through G-18 plus the Extended Compact Set #16–60) ran against the real, live, deployed pipeline — classifier, retrieval, and generation all unstubbed for the first time (Sprint 3's Golden Set run had to stub classification; that blocker resolved 2026-07-31) — see [docs/sprint-6-playbook-run.md](docs/sprint-6-playbook-run.md). That run surfaced and fixed two real defects: #42 (the classifier was over-firing `service_recovery` on plain requests like a late-checkout ask or a cancellation-fee question, short-circuiting straight to a canned escalation instead of answering) and #44 (the flagship anniversary relationship-bundle scenario silently never fired its `card` event, because the classifier's free-text occasion signal — "10th anniversary" — didn't exact-match the seeded `"anniversary"` tag). Both fixes live in `packages/prompts/classifier.md`. Each [PRD §20](docs/01-PRD-ai-concierge.md) Definition-of-Success line item is confirmed directly against the live system — see the Sprint Backlog's Sprint 6 section. The [Sales Demo Script](docs/13-sales-demo-script.md) rehearsal (all 5 beats) is **done**, driven live in-browser against the real running demo site, and surfaced four more real defects, all fixed: #45 (Beat 2's script named a "Romantic Escape" quick-start chip that [UX §2](docs/05-user-experience-flows.md) specifies but was never built — corrected the script to type the equivalent line instead), #46 (every concierge response rendered literal unparsed markdown instead of formatted text — a small renderer in `packages/ui`), #50 (Beat 2/3's recommendation cards silently collapsed to ~2px tall — a `WidgetShell` message-list child needed `flexShrink: 0`, easy to mistake for a stray divider), and #51 (Brand Settings' own Live Preview filled the whole header with the brand color, which the real widget never does — it now composes the actual `WidgetShell`/`ConciergeMessage`/`SuggestedChip` components instead of a hand-rolled lookalike). Only "give the meeting" itself remains, which isn't something this file tracks.

**Admin Portal visual redesign (2026-08-02–05, ad hoc, outside the sprint numbering above) is complete for all 9 admin screens — [PR #34](https://github.com/salmannausher/Hospitality-OS-/pull/34), [PR #35](https://github.com/salmannausher/Hospitality-OS-/pull/35), [PR #43](https://github.com/salmannausher/Hospitality-OS-/pull/43).** The admin shell (`(protected)/layout.tsx`) and every screen — Sign In, Dashboard, Conversations, Leads, Analytics, Relationships, Brand Settings, Notifications, Knowledge Base — went from Sprint 1/4's bare/unstyled inline-style scaffolding to real styled Next.js/Tailwind, using screen mockups produced in Stitch as the visual reference and ported by hand into code (Stitch's own `edit_screens`/`generate_variants` MCP tools proved unreliable mid-session and were abandoned in favor of reading each mockup's exported HTML/CSS directly). Reuses `apps/web`'s existing landing-page palette and fonts (`--color-ivory`/`ink`/`brass`/`champagne`/`night`, Fraunces + Instrument Sans) rather than inventing a third design-system token set — nothing in `docs/` has decided a dedicated admin design system, and this was the pragmatic, zero-new-dependency choice. A few honestly-grounded additions rode along, each computed from data already being fetched rather than fabricated: bundle service-counts and a real live guest-card preview on Relationships (turned out `previewRelationshipBundle` already called the real `CardAssemblyService` — the "live preview" just needed restyling, not building), Knowledge Base's Index Coverage % and real drag-and-drop upload, and Dashboard KPI tile icons. PR #43 also replaced Knowledge Base/Leads/Conversations' unpaginated fetches and Notifications' accumulating "Load more" with real prev/next cursor pagination (page size 10, shared `apps/web/src/app/admin/pagination.tsx`) plus a loading state on page transitions — deliberately not showing a fabricated "of N" total, since `Paginated<T>` carries no total count. Separately, a Codex-driven content/credibility review of the marketing landing page (`apps/web/src/components/landing/`) caught the Hero and "In Practice" demo implying capabilities the product doesn't have (multilingual support, post-stay engagement, PMS/booking-transaction actions) — corrected without touching the design system (same ivory/ink/brass tokens throughout).

**Sprint 7 (Onboarding Self-Service & the Remaining Admin Nav Items) is scoped, not started** — six tickets in the [Sprint Backlog](docs/14-sprint-backlog.md), after finding hotel onboarding has no self-service path past initial hotel creation + content upload (findings-log #47: no widget-key issuance endpoint/UI, no embed-snippet generator, no user-invite flow — an engineer runs scripts today) and that `Integration`/`Subscription`/`PromptOverride`/`AuditLog` are fully migrated tables with zero application code touching them (#48 — backing the four still-disabled nav items, "Prompt Settings"/"Integrations"/"Billing"/"Users & Roles").

**First production deployment (2026-08-04–05):** `api`, `web`, and `demo-bellevue` are now live on Vercel for the first time — see [PR #38](https://github.com/salmannausher/Hospitality-OS-/pull/38) for the build/runtime fixes that unblocked deploying at all. Getting the deployed widget actually visible surfaced #49 (the launcher/panel was invisible in production — `right`/`bottom` inline styles were silently dropped from the wrapper div in the bundled build specifically, isolated via a bare-iframe repro; worked around with a `useLayoutEffect` that re-asserts the inline style every render, root cause in a third-party animation library not chased further under deadline). Getting admin login actually working in production surfaced a real, separate gap: `apps/web`'s Vercel project had `NEXT_PUBLIC_API_URL`, `NEXT_PUBLIC_SUPABASE_URL`, and `NEXT_PUBLIC_SUPABASE_ANON_KEY` all unset (not stale — genuinely never populated), so the client-side admin login had no working path to either Supabase or the API at all; now set correctly and `web` redeployed. Separately, a `SUPABASE_SERVICE_KEY` value was inadvertently exposed in a terminal command's output during this diagnosis — the user rotated it in the Supabase dashboard same-day, the new value is live on `api`'s Vercel env (added via a direct file→stdin pipe, never displayed), and `api` was redeployed; login and a live guest chat both reverified working end to end afterward. **Not yet done:** a stable custom domain (both `api` and `web` currently only have Vercel's own project-level `*.vercel.app` aliases, e.g. `https://api-nine-chi-14.vercel.app` / `https://web-one-puce-88.vercel.app` — no `devsphinx.dev`/etc. domain wired up yet despite them being registered).

**Every blocker and behavior/spec finding — billing accounts, spec gaps, live-testing surprises — lives in one place: [docs/findings-log.md](docs/findings-log.md).** Read it before assuming something is broken, half-built, or undecided; it has the problem, the root cause, the fix actually chosen (and why, including rejected alternatives), and how it was verified. **Standing rule: log a new finding there *first*, the moment it's found — before fixing it, even in the same session.** Don't re-litigate an entry without reading it first. Current state in brief: one open billing item — #43, Voyage embeddings are still hitting free-tier rate limits (429s) under sustained live traffic despite an earlier note that this was resolved; flagged for the user to check the Voyage dashboard, not a code fix. Two items deliberately not yet built, scoped into Sprint 7 rather than fixed ad hoc — #47, #48 (see above). Everything else logged there is fixed or deliberately deferred with a stated reason (e.g. the extraction-prompt-quality gap on table columns like `capacity` — #32 — remains deliberately open, a prompt-tuning task, not a code fix). Two more small, user-flagged visual bugs are fixed as of 2026-08-05: #52 (the widget launcher's unclean corners — `Launcher.tsx`'s CSS-variable wrapper painted a sharp-cornered background behind the rounded pill; `WidgetShell` doesn't have this bug since its own root element already carries matching `border-radius` + `overflow: hidden`) and #53 (`demo-bellevue`'s hero text was nearly unreadable against its photo's bright sand — the bottom gradient faded to nothing before reaching the text; strengthened plus a text-shadow legibility floor added so it can't silently regress if the photo changes). Both merged via [PR #44](https://github.com/salmannausher/Hospitality-OS-/pull/44) and redeployed to production (`web` and `demo-bellevue` — `api` untouched, neither fix reaches it) — live at `https://web-one-puce-88.vercel.app` and `https://demo-bellevue.vercel.app`, confirmed via the deployed `widget.js` and a direct HTTP check, not just the local dev server.

**Demo property (Bellevue Hotel) site: built with the real concierge embedded** — `apps/demo-bellevue` (own workspace app, port 3002) implements the full homepage per [docs/18 creative direction](docs/18-demo-bellevue-creative-direction.md) and [docs/19 UX blueprint](docs/19-demo-bellevue-ux-blueprint.md), plus real subpages (`/rooms`, `/dining`, `/spa`, `/explore`, `/weddings`, `/reserve`) from [docs/16](docs/16-demo-property-content.md) content. Cormorant Garamond + Work Sans, sun-on-stone palette, brass accents, dark "evening" horizon sections bookending the page. The old `ConciergeWidget.tsx` visual stub is deleted; `layout.tsx` loads the real `packages/widget-embed` IIFE with `data-widget-key="wk_demo_bellevue"`, verified through grounded chat, escalation, contact capture, CTA, and programmatic reopen flows. Photography is hotlinked from verified Unsplash IDs in `src/lib/images.ts`; owned local photography assets remain future work. Known placeholders: contact emails in `content.ts`/`Footer.tsx`, and three testimonial quotes authored for this demo (flagged in [docs/19 §8](docs/19-demo-bellevue-ux-blueprint.md) — not in docs/16, don't mistake them for real guest quotes).

**Landing page: built (second pass, own editorial design) — awaiting user sign-off.** Lives at the `/` route of `apps/web` (`src/components/landing/`), per [docs/17 §5](docs/17-landing-page-plan.md). It deliberately does **not** reuse Option A's product tokens (the first pass did, and was rejected as "not premium"): it has its own language — Fraunces + Instrument Sans, ivory/ink + brass/champagne, dark "evening" conversation-demo and closing sections, restrained `motion`-powered scroll reveals. Known placeholders: the contact email in `src/components/landing/constants.ts`, and the "See it live" demo is a staged animation until the Bellevue site (Sprint 5) exists.

## Environment gotchas — learned by actually building, not in the docs above

- **Node version:** the default shell's `node` is ancient (v15). Always run `export NVM_DIR="$HOME/.nvm" && . "$NVM_DIR/nvm.sh" && nvm use 24` before any `pnpm`/`node`/`prisma` command — shell state doesn't persist between tool calls, so this needs repeating every time, not just once per session.
- **Outbound DB connections need the sandbox disabled** for this environment specifically (`dangerouslyDisableSandbox: true` on the Bash call) — plain TCP tools like `nc` can succeed while Prisma/`pg` still can't reach the same host, because the sandbox restricts it per-process, not at the network level. If a Prisma command reports `P1001` (can't reach database server) but you're confident the credentials/host are right, this is the first thing to check, not the last.
- **Prisma is v7, not v5/6** — several things changed from what the docs originally assumed (both corrected already, but worth knowing if a stale mental model creeps back in): connection URLs live in `apps/api/prisma.config.ts`, not `schema.prisma`'s `datasource` block; several CLI flags were renamed (`--to-schema-datamodel` → `--to-schema`, `prisma db execute` no longer takes `--schema`).
- **Two Postgres connection strings, not one**, both via Supabase's pooler host (not the `db.<ref>.supabase.co` direct host — that one is IPv6-only and unreachable from most sandboxed environments): `DIRECT_URL` (port `5432`, session pooler, connects as the `postgres` owner role — used for migrations) and `DATABASE_URL` (port `6543`, transaction pooler, connects as the restricted `app_role` — used for actual runtime queries). Never swap these.
- **The `postgres` role owns every migrated table and bypasses RLS by default** — this is exactly why `app_role` (`NOSUPERUSER NOBYPASSRLS`, migration `2_app_role`) exists and why runtime code must connect as it, never as `postgres`.
- **Never run a destructive Prisma command (`migrate reset`, etc.) without the user's explicit, in-the-moment consent** — Prisma's own CLI detects an AI agent and refuses to run these without it. If drift or a reset is ever needed again, either ask the user to run it themselves in their own terminal, or get explicit written consent before running it yourself.
- **Supabase auto-installs `pgcrypto`, `pg_stat_statements`, `supabase_vault`, `uuid-ossp` on every project** — these are declared in `schema.prisma`'s `extensions` list alongside `vector` so Prisma's schema comparisons expect them, but `migrate dev`'s shadow database has neither them nor the `vault` schema, so it reliably fails (`P3006` / `schema "vault" does not exist`). **This is not a one-time first-migration thing — it recurs for every new migration** (hit again adding migration `6_document_validation_issues`, not just `0_init`). Never run `migrate dev` against this database. The working flow every time: `prisma migrate diff --from-config-datasource --to-schema=prisma/schema.prisma --script` → hand-review the output (see next point) → save the reviewed SQL as `migrations/<n>_<name>/migration.sql` → `prisma db execute --file <that path>` (no `--url` flag in this Prisma version — it reads `prisma.config.ts`'s `DIRECT_URL` automatically) → `prisma migrate resolve --applied <n>_<name>`.
- **That same diff will also propose `DROP INDEX` on hand-authored indexes Prisma's schema DSL can't express** — migration `1_rls_policies` added a GIN index on `domainTags` columns and an HNSW index on `Chunk.embedding` via raw SQL, invisible to `schema.prisma`, so every subsequent `migrate diff` sees them as drift to delete (`chunk_domain_tags_gin_idx`, `chunk_embedding_hnsw_idx`, `message_domain_tags_gin_idx`). Never apply those statements — hand-edit the generated SQL down to only the actually-intended change before running `db execute`.
- **`nest build`/`nest start --watch` output goes to `dist/`, and some scripts (`prisma/ingest-bellevue.mjs`) `require()` from `dist/` directly, not the TS source** — a source edit with no rebuild silently keeps running the old compiled behavior. If a live-verification result doesn't match a just-made code change, rebuild (`pnpm run build`) before assuming the fix didn't work.

## Decisions already made — don't re-litigate or default around these

- **Model:** Claude via the AI Gateway, **not** OpenAI. **Embeddings:** Voyage AI, **not** `text-embedding-3-small`. ([Architecture §7](docs/06-system-architecture.md), [AI Engine §1](docs/10-ai-engine-specification.md))
- **Auth:** Supabase Auth issues all credentials end to end. There is no custom `/auth/login`, no app-issued access/refresh token pair, no password ever touches our database. ([API §3.1](docs/09-api-specification.md), [DB §1](docs/07-database-design.md))
- **Deployment:** single-platform Vercel (Fluid Compute), **not** Railway/Fly, **not** Docker. NestJS runs behind a single catch-all Vercel Function. ([Architecture §8](docs/06-system-architecture.md))
- **Queue:** Upstash Redis + BullMQ, **not** Vercel Queues (revisit only once Vercel Queues is GA and the swap is genuinely cheap — it's behind an interface for exactly this reason). ([Architecture §8](docs/06-system-architecture.md))
- **Multi-tenancy:** Postgres row-level security keyed on `hotelId`, set server-side per request — never a client-supplied tenant id, never a query that skips the filter. ([DB §9](docs/07-database-design.md))
- **Roles:** `SUPER_ADMIN | AGENCY_ADMIN | HOTEL_ADMIN | MARKETING | RESERVATIONS | VIEWER` — exactly these six, nothing else. ([DB §3](docs/07-database-design.md))
- **Testing:** the [Playbook](docs/04-conversation-playbook.md) is the end-to-end regression suite; unit tests are for the deterministic pure functions specifically (confidence formula, rerank score). No blanket coverage target, no separate eval framework to build.

## How to work in this repo

- **Modular monolith** — `apps/web` (Next.js), `apps/api` (NestJS), shared `packages/{ui,types,prompts,sdk,config}`. Not microservices. ([Architecture §3](docs/06-system-architecture.md), [Engineering Conventions §2](docs/12-engineering-conventions.md))
- **API-first** — the frontend calls the backend only through `packages/sdk`, matching [API Specification](docs/09-api-specification.md) exactly. Never a direct Prisma call from a Next.js page.
- **Work off [docs/14-sprint-backlog.md](docs/14-sprint-backlog.md)**, not the more abstract phase descriptions in the Development Plan — it's the live checklist; check boxes off as work completes, don't just read it.
- **For the prompt library specifically**, use [docs/15-prompt-library-implementation-prompts.md](docs/15-prompt-library-implementation-prompts.md) directly — it's already broken into ready-to-run, self-contained tasks.
- **Before generating code for a ticket:** state the plan and which doc section(s) it implements. **After generating:** state what was created and where. **Output complete files, never partial implementations.**
- **No duplicated business logic.** If a rule (grounding, escalation, lead-capture signals) needs to apply in more than one place, it lives once — in `packages/prompts/base.md` or the relevant shared module — and gets referenced, never re-typed.
- **Every ticket's Definition of Done** is the standing checklist in [Sprint Backlog — "Per-Ticket Definition of Done"](docs/14-sprint-backlog.md), regardless of which sprint it's in.

## Housekeeping

- Run a **weekly refactor pass**: duplicated code, files that have grown too large, dead imports, drift from the module boundaries above. Suggest improvements; don't rewrite unless the drift is real.
- **Bug fixes:** identify root cause and affected modules before patching, fix without breaking the architecture above, preserve API compatibility, return only the modified files.
- **Git:** PR-only merges, even solo — see [Engineering Conventions §8](docs/12-engineering-conventions.md).
