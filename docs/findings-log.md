# Findings & Blockers Log

**This is the single source of truth for every blocker and behavior/spec finding on this project — billing/account blockers and real findings alike.** CLAUDE.md and the [Sprint Backlog](14-sprint-backlog.md) point here instead of duplicating the narrative.

**Standing rule: every new finding or blocker gets an entry here *first*, the moment it's discovered** — before it gets fixed, and even if it's fixed in the same session. Backfilling this log later is not the workflow; logging as you go is. A one-line pointer from CLAUDE.md's status section or a Sprint Backlog ticket is fine, but the actual problem/root-cause/fix narrative lives here, once, not copied across three files that can drift out of sync.

## Entry format

```
### N. Title — STATUS

**Found:** sprint/date, what surfaced it (a ticket, a live test, a Golden Set scenario, an account notification)
**Problem:** what broke, was missing, or blocked progress
**Root cause:** why it happened
**Fix chosen — and why:** what was actually done, the alternatives that were considered and rejected, and the reasoning
**Verification:** how it was proven (a script, a live test, a PR)
```

`STATUS` is one of: **OPEN** (known, not yet fixed) · **FIXED** (resolved, verified) · **DEFERRED** (a deliberate decision not to fix now, with a stated reason) · **MITIGATED** (partially addressed, full fix still open).

---

### 1. AI Gateway free-tier billing restriction — OPEN

**Found:** Sprint 1, reconfirmed throughout Sprint 2/3's live verification scripts.
**Problem:** The AI Gateway account reverted to free-tier restrictions (`Free tier users do not have access to this model`) despite a card on file, blocking real live classifier calls (haiku) and real ingestion-time entity extraction. Generation (sonnet-5) works fine — only classifier/extraction-tier model access is restricted.
**Root cause:** Account billing state, not code — the $ shown in the Vercel dashboard is unused free starter credit, not a purchase. A card on file alone doesn't unlock restricted models; an actual credit purchase does.
**Fix chosen — and why:** Not a code fix — every Sprint 2/3 verify script stubs `GatewayService.classify`'s *output* to the exact classification a working classifier would produce for the scenario under test, so the rest of the pipeline (retrieval, scoring, generation, card/escalation/lead/cta logic) is exercised for real. This was chosen over waiting to build/verify anything classifier-dependent, since the blocker is the user's own pending action, not something code can work around. `verify-gateway.mjs` is the direct probe — rerun it any time to check if this has resolved.
**Verification:** `apps/api/verify-gateway.mjs` (classifier `FAIL` / generation `PASS` as of last check). Not resolved — don't nag the user about it, they're aware and have deferred it.

### 2. Voyage embeddings rate-limiting — FIXED (2026-07-23)

**Found:** Sprint 3, while live-verifying the `card` SSE event (ticket 3).
**Problem:** Voyage embeddings started returning `429`s — "You have not yet added your payment method... reduced rate limits of 3 RPM and 10K TPM."
**Root cause:** Voyage's policy: 200M free tokens apply regardless of payment method, but *without* one, rate limits are heavily restricted; adding a payment method unlocks standard limits, with the free allowance still stacking on top. No payment method was on file.
**Fix chosen — and why:** User added a payment method (their own account action, not a code fix). Confirmed via a direct unstubbed `voyage-4` call returning a real `200` and a real embedding vector.
**Verification:** Direct `fetch` probe against `https://api.voyageai.com/v1/embeddings` — `200`, real embedding returned. Re-ran the ticket-3 verify script's fully-unstubbed path afterward with no change in behavior (confirming the fix didn't regress anything).

### 3. Classifier domain under-tagging vs. IA §7's hard-filtered retrieval — MITIGATED

**Found:** Sprint 1, live testing.
**Problem:** Asking "What time is breakfast served?" produced a fluent but wrong "I don't have that information" answer. The classifier tagged the message `domain: ["accommodation"]` (should have included `dining`/`policies`), and retrieval hard-filters on domain overlap (IA §7's explicit, documented "mandatory, not optional" design) — so the correct breakfast chunk never entered the candidate set. Confidence still computed MEDIUM (not LOW) on whatever *did* get retrieved, so generation ran instead of the safe fallback.
**Root cause:** The pipeline did exactly what it was built to do — an imperfect classifier tag combined with a hard filter that has no fallback path.
**Fix chosen — and why:** Discussed with the user first (three options proposed). Chose the smallest one: `packages/prompts/classifier.md` now explicitly instructs the classifier to tag every domain a query plausibly touches, not just the single best-fit one, with the breakfast case spelled out as a worked example. Rejected, for now: (a) domain as a rerank *boost* instead of a hard predicate, (b) a fallback retry without the domain filter on LOW/empty results — both are real architecture changes to IA §7's retrieval design, not something to decide unilaterally mid-fix.
**Verification:** Prompt change shipped 2026-07-22; live re-verification of the specific breakfast query was blocked on the AI Gateway restriction (finding 1) at the time. If the gap resurfaces once that's resolved, the belt-and-suspenders fallback-retry option is the next step, not the rejected soft-filter option.

### 4. Needs-Review edit form has no way to trace a validation issue to its source entity — DEFERRED

**Found:** Sprint 2, while building the admin knowledge upload surface.
**Problem:** UX §9's guided, pre-filled Needs-Review edit form needs to trace a validation issue back to the specific entity row it came from (e.g., "Ocean Suite is missing capacity" → jump straight to editing that `RoomType` row). None of the nine entity tables has a link back to the document/extraction it came from.
**Root cause:** A schema gap — only `Chunk` has a `documentId` link; the nine structured-entity tables (`RoomType`, `Restaurant`, etc.) don't.
**Fix chosen — and why:** Deliberately not built. Adding a `documentId` FK is a migration across all nine entity tables — a real architecture decision (does every entity need this, or only ones created via extraction vs. admin CRUD?), not surface work to bolt on while building the upload screen. `Document.validationIssues` ships as read-only in the meantime, so admins can see *what's* wrong even though they can't yet jump to fixing it from a pre-filled form.
**Verification:** N/A — deliberately unbuilt. Revisit when the guided edit form itself is scoped (likely Sprint 4 admin portal work).

### 5. `RecommendationCard`'s `title`/`hook` had no source field anywhere — FIXED (Sprint 3, ticket 2)

**Found:** Sprint 3, while implementing `POST /v1/admin/relationships/preview`.
**Problem:** The card preview endpoint needs to return `{ title, hook, imageUrl?, linkUrl? }` per entity, but no entity table or `EntityRelationship` row has a field for authored copy. UX §3/API §2.1 reference "the entity's own hook" as if it already exists; it doesn't.
**Root cause:** A genuine, undocumented gap — checked DB §6, API §2.1/§3.3, UX §3/§10, AI Engine §1/§4; none specify where this copy comes from.
**Fix chosen — and why:** Asked the user directly (three options: deterministic template / schema addition to `EntityRelationship` / schema addition to all nine entity tables). User chose the deterministic template: `title` = the entity's own name/topic field; `hook` = a string templated per entity type from *existing* fields (e.g. RoomType → `"ocean view · sleeps 4"`), no schema change, no model call — consistent with AI Engine §1's call inventory (no card-assembly model call) and §4's deterministic reranking. `imageUrl`/`linkUrl` stay `null` (already optional in the type). Rejected: both schema-addition options, since real admin-authored bundle copy is a bigger, separate decision not needed to unblock this ticket.
**Verification:** `apps/api/verify-relationships.mjs` (15/15 checks) — confirmed titles/hooks render correctly for both a `name`-keyed entity (RoomType) and the one type keyed differently (`Policy.topic`).

### 6. `cta` event had no URL source anywhere in the schema — FIXED (Sprint 3, ticket 6)

**Found:** Sprint 3, while implementing the `cta` SSE event.
**Problem:** Every `cta` kind requires a `url: string`, but nothing in `Hotel`/`BrandSettings`/`PropertyProfile` holds a booking-engine link, a rooms page, or any other CTA target.
**Root cause:** No hotel-configurable link field exists anywhere in the schema — genuinely undecided, not an oversight in one place.
**Fix chosen — and why:** Asked the user directly (three options: add one configurable field / ship a placeholder URL pattern / defer the whole ticket). User chose adding `BrandSettings.bookingEngineUrl` (migration `7_brand_settings_booking_url`, additive/nullable). `book_now` and `explore_rooms` both point to it (realistic — most real hotel sites route both into the same booking engine). `plan_my_stay` falls back to the same URL as an explicitly-flagged interim decision (a dedicated itinerary/local-guide URL is a separate, real gap). `request_assistance` never uses it — UX §6 frames that CTA as triggering in-widget escalation, not a link-out. Unconfigured → empty string, never a fake link. Rejected: the placeholder-URL option (too risky for the upcoming Bellevue demo work) and deferring the whole ticket (a small, real schema addition was cheap and unblocked everything else).
**Verification:** `apps/api/verify-cta.mjs` (24/24 checks) — configured-vs-unconfigured behavior for all four CTA kinds, live against the real Bellevue hotel.

### 7. Missing classifier signals for two ABS §7/Playbook §6 concepts — FIXED (Sprint 3, tickets 5 & 6)

**Found:** Sprint 3, while implementing escalation (ticket 5) and the `cta` event (ticket 6).
**Problem:** Two documented behaviors had no classifier signal to detect them: (a) ABS §7's "explicit request to talk to a person" is independent of `journeyState: service_recovery`, but `ClassifierOutput` had no field for it; (b) Playbook §6's Trip Lifecycle Stage (`dreaming → researching → comparing → booking → preparing → staying`) is a second axis from `journeyState`, and nothing captured it at all.
**Root cause:** Both concepts were fully specified in the docs but never wired into the classifier's structured output schema.
**Fix chosen — and why:** Extended `ClassifierOutput.detectedSignals` with `explicitHandoffRequest: boolean` and `lifecycleStage: LifecycleStage`, both inferred from explicit language in the message or conversation history per updated `classifier.md` instructions, each with a safe, documented default when ambiguous (`false` / `"researching"`). Chosen over keyword-matching heuristics (fragile, not real detection) or leaving the behaviors unbuilt — the Playbook's own text explicitly says lifecycle stage "can often be inferred from explicit language... without new infrastructure," which is exactly what this does.
**Verification:** `apps/api/verify-escalation.mjs` and `apps/api/verify-cta.mjs` — both signals exercised live end to end through `ChatService.streamTurn`.

### 8. Card/text divergence — the two recommendation surfaces didn't cross-reference each other — FIXED (2026-07-26)

**Found:** Sprint 3's full Golden Set run (G-05) — see [the run log](sprint-3-golden-set-run.md).
**Problem:** The `card` SSE event correctly returned the real seeded `anniversary` bundle (Ocean View Suite + The Rooftop at Bellevue + Couples Massage), but the *generated answer text* recommended a different restaurant — The Terrace — for dinner. The guest would see a card naming one restaurant and read a paragraph naming another, in the same turn.
**Root cause:** Card assembly (`EntityRelationship` lookup) and answer generation (vector retrieval → RAG context) were two fully independent systems with no cross-referencing — retrieval had no idea what the card mechanism had already picked.
**Fix chosen — and why:** `ChatService` now resolves the relationship bundle *before* generation (moved up from after), and — when one fires — injects a "you must recommend precisely these entities" instruction into the system prompt, folded into the existing `{{rag_context}}` slot rather than a new `base.md` placeholder (`base.md`/ABS §14 is a shared prompt-template contract, deliberately out of scope for a targeted fix — see finding 9). The already-resolved bundle is reused for the `card` event itself instead of being queried a second time — one lookup, not two. Considered and rejected: biasing vector retrieval toward the bundle's entities instead (more invasive, touches the shared retrieval scoring path for a fix that's really about generation-time instructions).
**Verification:** `apps/api/verify-card-text-consistency.mjs` — reproduces the exact G-05 scenario; text now names "The Rooftop at Bellevue," matching the card. All five other chat-pipeline verify scripts re-run clean afterward — zero regressions.

### 9. Escalation fires for the wrong documented reason on wedding/events inquiries — FIXED (2026-07-26)

**Found:** Sprint 3's full Golden Set run (G-09, G-10) — see [the run log](sprint-3-golden-set-run.md).
**Problem:** Both scenarios did escalate, but via the `low_confidence` trigger (Bellevue has zero indexed `events`-domain content, so retrieval returns empty) rather than ABS §7's "group/event size threshold" trigger. The `Escalation` row's logged `reason` (`low_confidence`) didn't reflect the real cause (a high-value group inquiry), which would misdirect any reason-based analytics later (API §3.6's planned "grouped by structured `reason`" view).
**Root cause:** Two compounding gaps: (1) the real group/event-size-threshold trigger was never built — "configurable size threshold" implies a per-hotel setting that didn't exist anywhere in the schema; (2) no `events`-domain content is seeded for Bellevue, so even a correct trigger would have nothing to ground a helpful capacity/AV-facts answer with (this half is a content gap, not something a code fix resolves — see "What this run does NOT cover" in the run log).
**Fix chosen — and why:** Added `BrandSettings.groupInquiryThreshold` (`Int @default(15)`, migration `8_brand_settings_group_threshold`) — same reasoning as `bookingEngineUrl` (finding 6): a small, per-hotel-configurable field rather than a hardcoded number. Added a new classifier signal, `detectedSignals.groupSize`, populated only when the guest states a specific guest/attendee count for a group/wedding/event booking (never for an ordinary party-size mention). Architecturally, this trigger is evaluated differently from `service_recovery`/`explicit_request`: re-reading Playbook G-09's own expected behavior showed it wants the concierge to *engage first* — share `Event Space` capacity facts, ask a clarifying question (exactly what `wedding.md`'s module instructions already say) — not skip generation entirely like the other two triggers. So `group_size_threshold` is checked *after* generation completes (or after the Low-Confidence fallback fires), not before, and takes priority as the *reported* reason over an incidental `low_confidence` whenever both apply — since it's the more specific, structured signal staff actually need. `service_recovery`/`explicit_request` still take priority over it if a message somehow trips more than one trigger.
**Verification:** `apps/api/verify-group-size-escalation.mjs` (9/9 checks) — reproduces G-09 and G-10 exactly (now `reason: group_size_threshold`), confirms below-threshold group sizes don't trigger it, confirms a real generated answer streams before the escalation fires when real content exists (proving the "engage first" behavior), confirms the per-hotel threshold is actually configurable, and confirms `service_recovery` still wins if both signals are present. All 6 other chat-pipeline verify scripts re-run clean afterward — zero regressions.

### 10. `base.md` never received ABS §10/§19's refusal-table content — FIXED (2026-07-26)

**Found:** Sprint 3's full Golden Set run (G-12) — see [the run log](sprint-3-golden-set-run.md).
**Problem:** The actual system prompt template (`packages/prompts/base.md`) — which correctly and deliberately copies ABS §14 verbatim — never included ABS §10's refusal rules (competitor comparisons, prompt-extraction, medical/legal/financial, harassment) or the ABS §19 forbidden-behaviors checklist anywhere in its text.
**Root cause:** Not a code bug — `base.md` faithfully matched the spec section it was supposed to (§14). The gap was *between two spec sections*: §10 and §19 documented required refusal behaviors that §14 (the section actually wired into the running system) never referenced. Made concrete by G-12: a competitor-comparison question scored Low Confidence on retrieval and never even reached generation, so neither an explicit instruction (none existed) nor the model's own default judgment got a chance to apply. G-13 (prompt-extraction) succeeded — but on the model's own default alignment, not anything this system actually specified, so that success was incidental, not guaranteed.
**Fix chosen — and why:** Fixed the spec at its source, then propagated — the same discipline the Prompt Library's own Prompt 0 established ("copy verbatim... flag rather than guess"). Added one new paragraph to **[ABS §14](02-ai-behavior-specification.md)** (not just `base.md` directly) covering §10's six refusal categories plus the two §19 bullets not already covered elsewhere in the template (claiming a completed booking; sharing one guest's info with another — every other §19 bullet was already implied by existing template text). `packages/prompts/base.md` then updated to match §14 verbatim, same as always. `registry.ts`'s `"base"` entry bumped to `v2` with expanded `playbookCoverage` (`G-12`, `G-13`, and Adversarial extended-scenario items 56/57/59/60). No application code changed — this is a prompt-content-only fix, unlike findings 8/9.
**Verification:** `apps/api/verify-refusal-content.mjs` (6/6 checks) — reproduces G-12, G-13, and Playbook items 56/57/59/60 live. **Honest result, not assumed:** only G-13, 57, and 59 actually reached generation and exercised the new instruction for real, each with a specific, correct refusal (declined plainly, refused to share a room number, disengaged from harassment without lecturing). G-12, 56, and 60 never reached generation at all — Low Confidence intercepted them first, same as before this fix — so they only "passed" because the generic Low-Confidence fallback text is itself safe, not because the new content did anything. See finding 11. All 7 other chat-pipeline verify scripts re-run clean afterward — zero regressions.

### 11. Off-topic/refusal-category messages can be intercepted by Low Confidence before any refusal instruction ever applies — FIXED (2026-07-26)

**Found:** Verifying the fix for finding 10 (`apps/api/verify-refusal-content.mjs`).
**Problem:** Fixing `base.md`'s content gap (finding 10) did not guarantee the new refusal instructions ever got a chance to run. Three of six reproduced scenarios (G-12 competitor comparison, item 56 roleplay/jailbreak-for-a-password, item 60 repeated discount pressure) scored Low Confidence on retrieval and were routed to the honest-fallback text *before generation ever ran* — the exact same failure mode finding 10 originally exposed via G-12, confirmed to apply to an entire category of message, not just that one scenario.
**Root cause:** These messages are semantically off-topic relative to the hotel's actual indexed content (a competitor's name, a fake roleplay premise don't resemble real hotel-content chunks), so they score genuinely low similarity — the confidence gate is doing exactly what it's designed to do (AI Engine §5: "Low: Do not answer from the model"). The gate has no way to distinguish "we have no knowledge-base answer for this" from "this is a refusal-category message that doesn't need retrieval at all" — both look identical to it: low similarity, no strong chunk match.
**Fix chosen — and why:** Added `detectedSignals.offTopicOrRefusal: boolean` to the classifier's structured output (same pattern as `explicitHandoffRequest`/`groupSize`, findings 7/9), true for five of ABS §10's six categories — competitor comparison, general off-topic, prompt-extraction, medical/legal/financial-advice, harassment. `ChatService`'s confidence gate (`if (band === 'LOW' || topChunks.length === 0)`) now also requires `!offTopicOrRefusal` — generation always runs for these categories regardless of retrieval confidence, trusting the finding-10 content to handle them (with whatever context happens to exist, possibly none). Deliberately, no `escalationReason` gets set in this path either — a competitor-comparison redirect isn't an escalation-worthy "we don't know" moment. **Scope decision made during planning:** the sixth ABS §10 category — policy-override/discount requests — is deliberately excluded from this signal. ABS §10 itself says the correct response there is to "explain what it *can* do (e.g., check current promotions)," which is a real retrieval question (does a current promotion exist?), not a refusal that should skip retrieval — it keeps going through the normal confidence-gated path exactly as before (already safe: `base.md`'s existing grounding rule already prevents inventing a discount, whichever path handles it). Multi-turn "escalate if harassment continues" (ABS §10) is explicitly out of scope — no signal today captures "was this already flagged in an earlier turn."
**Verification:** `apps/api/verify-refusal-content.mjs` updated and re-run (8/8 checks, up from 6) — G-12 and item 56 now produce real, specific, generated refusals (not the generic fallback string) even while `confidenceBand` genuinely reports `LOW`, confirming the bypass works without corrupting the confidence signal itself. Item 60 (discount) confirmed unchanged, still on the normal path. All 7 other chat-pipeline verify scripts re-run clean — zero regressions.

### 12. `DailyMetric` has no writer anywhere in the codebase — FIXED (2026-07-27)

**Found:** Sprint 4, starting ticket 1 (dashboard KPI tiles, API §3.6).
**Problem:** `GET /v1/admin/analytics/daily` is specified to read `DailyMetric` rollups, never live aggregates (API §3.6's own words: "Reads `DailyMetric` rollups... Powers KPI tiles"). Nothing in `apps/api/src` — no service, no cron, no `@nestjs/schedule`/queue job — has ever written a `DailyMetric` row. The table is migrated and RLS-scoped like every other tenant table, but reading it today returns nothing for any hotel, for any date, ever. The DB §13 model comment itself only says it's "computed once per hotel per day (batch job or incrementally on write)" — leaving the choice open, not specifying which, and no ticket anywhere in the Sprint Backlog names a rollup job as its own line item.
**Root cause:** A genuine, previously-unnoticed gap between the DB design doc (which defines the table and assumes something populates it) and every sprint that actually touched conversations/leads/escalations (1 and 3) — none of them were told to also maintain this rollup, since it wasn't yet in scope until this ticket.
**Fix chosen — and why:** Incremental-on-write, not a batch job. No cron/queue infrastructure exists yet in this codebase (BullMQ/Upstash is still an in-process stub per the Sprint 2 entry above), and Sprint 4's own Definition of Done is explicit that this sprint introduces "no new backend concepts, only surfaces" — standing up a scheduler to satisfy one ticket would violate that. Instead, a small shared pure function (`bumpDailyMetric`, matching the existing `scoring.ts`/`cta.ts` convention of deterministic, unit-testable helpers) is called from inside the *already-existing* `withTenant` transactions in `ChatService` (message + conversation + booking-intent counts), `LeadsService` (lead count, only on a newly-created, consented Lead), and `EscalationsService` (escalation count, plus lead count on its own contact-capture branch). This is instrumentation added to surfaces that already exist, not a new concept — the alternative (a nightly batch job recomputing from raw tables) was rejected as both a new piece of infrastructure this sprint explicitly says not to add, and strictly worse at "1,000+ hotels" scale per the DB §13 comment's own reasoning for why this table exists at all.
**Verification:** `apps/api/verify-dashboard-analytics.mjs` (18/18 checks) — real chat turns (via `ChatService.streamTurn`), `LeadsService.submitAnswer`, and `EscalationsService.create`/`choose` all land correct deltas in `DailyMetric`, `GET`-equivalent `AnalyticsService.getDaily`/`AdminAnalyticsController.daily` return them correctly for a date range (including date-range exclusion, defaulting, and validation), and a live browser session (real Supabase Auth login, real `/v1/admin/analytics/daily` call) rendered the Dashboard KPI tiles end to end with real numbers. All 4 other chat-pipeline verify scripts (`verify-lead-capture`, `verify-escalation`, `verify-group-size-escalation`, `verify-chat-cards`) re-run clean afterward — zero regressions. **Caught and fixed during that regression pass:** `ChatService.openTurn` originally added a `findUnique` before the `conversation.upsert` to detect "is this a new conversation" — a real, if modest, added-latency risk in a transaction already documented as running close to Supavisor's 5s interactive-transaction timeout under this sandbox's non-colocated network path (see the Environment Gotchas section of CLAUDE.md). Reworked to reuse the `prior` messages array already being fetched for conversation history instead (a brand-new conversation has zero prior messages by definition) — same correctness, zero added round trips.

**Known follow-up gap, not fixed here:** `avgSatisfaction` has no capture mechanism anywhere in the product (no guest-facing rating prompt exists in any doc's UX flow) — it stays `null` for every row. The Dashboard's "Guest Satisfaction" tile shows "No data yet" rather than a fabricated number. Building a real satisfaction-capture flow is a separate, undecided feature, not something to invent silently to fill this tile.

### 13. Dashboard's "Answered w/o Handoff %" tile could render negative — FIXED (2026-07-27)

**Found:** Sprint 4 ticket 1, live-verifying the Dashboard in-browser after finding 12's fix.
**Problem:** UX §8's formula (`1 - escalationCount/conversationCount`) rendered `-75%` in a real browser session, because that day's seeded/test `DailyMetric` row had more escalations (7) than new conversations (4) — a real scenario, not just test-data noise: `escalationCount` counts every escalating turn, `conversationCount` only increments once per new conversation, so more than one escalation inside the same conversation on the same day makes escalations exceed conversations.
**Root cause:** The UX §8 mock's own worked example (128 conversations, 3 escalations) implicitly assumes escalations never exceed conversations — a real but unstated assumption the formula itself doesn't enforce.
**Fix chosen — and why:** Clamped the computed percentage to a minimum of 0 in `apps/web/src/app/admin/(protected)/page.tsx`'s `buildTiles()` — a one-line display-layer fix, not a change to what's stored (the raw counts are correct and meaningful on their own; only the *derived* percentage needed guarding against a nonsensical negative). Rejected: changing what `escalationCount`/`conversationCount` mean, which would be a bigger, unrelated change to a metric other future Analytics work (API §3.6) also depends on.
**Verification:** Confirmed live in-browser — reloaded the Dashboard against the same real (7 escalations / 4 conversations) data and it now shows `0%` instead of `-75%`.

### 14. `Message.escalationTriggered`/`leadCaptureTriggered` never get set — FIXED (2026-07-27)

**Found:** Sprint 4, starting ticket 2 (conversation list + thread view + QA scoring, API §3.4).
**Problem:** API §3.4's `GET /v1/admin/conversations/:id` is specified to return "messages with their per-turn journeyState, confidenceBand, **flags**" — `Message.escalationTriggered`/`Message.leadCaptureTriggered` are exactly those flags (DB §10). `ChatService.persistConciergeTurn` writes `journeyState`, `domainTags`, and `confidenceBand` onto every concierge message, but never touches either boolean — they're stuck at their schema default (`false`) on every message ever written, even ones where an escalation or lead prompt genuinely fired that turn.
**Root cause:** Both booleans were added to the schema for exactly this thread-view use case, but nothing in Sprint 1 or 3's chat-pipeline work was ever told to populate them — `streamTurn` already computes both facts (`escalationReason`, and whether a `lead_prompt` event fires) for its own SSE-event logic, it just never threads them through to the persisted row.
**Fix chosen — and why:** Thread both facts through to `persistConciergeTurn` as two more fields, computed from state `streamTurn` already has (`escalationReason !== null`; a new `leadPromptFired` local set at the exact point the `lead_prompt` event is yielded) — no new detection logic, just wiring existing signals to the row they were always meant to land on.
**Verification:** `apps/api/verify-conversations.mjs` (30/30 checks) — a real service_recovery turn shows `escalationTriggered: true`/`leadCaptureTriggered: false` on its concierge message; a real booking_intent+leadCaptureWorthy turn shows the inverse. All 3 other chat-pipeline verify scripts re-run clean afterward.

### 15. `Lead` has no `source` column, but API §3.4 assumes one — FIXED (2026-07-28)

**Found:** Sprint 4, starting ticket 3 (Leads inbox, including manual entry, API §3.4).
**Problem:** API §3.4's own words: `POST /v1/admin/leads` is "manual lead entry... same `Lead` model, `source: "manual"`" — implying a `source` field distinguishing manually-entered leads from chat-captured ones. `Lead` (DB §10/§11) has no such column at all.
**Root cause:** The API spec was written assuming a field that was never added to the schema — the same kind of doc/schema gap findings 5, 6, 12, and 14 already surfaced elsewhere in this project.
**Fix chosen — and why:** No migration needed — `Lead.conversationId` already distinguishes the two cases without a new column. Every lead created through the chat pipeline (`LeadsService.submitAnswer`, `EscalationsService.captureContact`) always sets `conversationId` (it's the whole point of both flows — capturing contact info *for a conversation*); a manually-entered lead has no conversation behind it at all. So `source` is computed at read time (`conversationId == null ? "manual" : "chat"`) in the admin API response, the same "derive it, don't store it redundantly" approach ticket 2 used for `ConversationSummary.domainTags`/`journeyState`. Rejected: adding a `Lead.source` enum column — a real schema change for a fact `conversationId`'s existing nullability already encodes.
**Verification:** `apps/api/verify-leads-inbox.mjs` (21/21 checks) — a chat-captured lead reports `source: "chat"` with its real `conversationId`; a manually-created one reports `source: "manual"` with `conversationId: null`. Also covers `list` (status filter + cursor pagination), `get` (404 on unknown id), and `update` (status validation, `assignedOwnerId` validated against a real `HotelMembership` — rejects an id with no membership for the hotel — notes, and clearing either field with an explicit `null`). Live-verified in-browser too: manual entry form and the status/owner/notes edit form both round-tripped for real (`POST`/`PATCH` → `201`/`200` in the network log), status filter dropdown correctly narrowed the list. All 3 other chat-pipeline verify scripts re-run clean — zero regressions.

### 16. Leads Inbox table header cells ran together with no spacing — FIXED (2026-07-28)

**Found:** Live browser verification of the Leads Inbox page, ticket 3.
**Problem:** A screenshot showed table headers rendering as "SourceStatus", "OwnerCreated" — no visible gap between adjacent columns' text.
**Root cause:** Copied the Knowledge Base/Conversations pages' own table convention, which only puts `padding` on the *first* `<th>`/`<td>` in each row. That convention happened to look fine there because the first column's text ("Document", a guest's contact/topic tags) was wide enough to create visual separation on its own — Leads' narrower columns (Source, Status, Owner) exposed the gap the convention never actually closed.
**Fix chosen — and why:** Gave every `<th>`/`<td>` in the Leads table its own right padding (`0.5rem 1rem 0.5rem 0`), rather than relying on incidental whitespace from neighboring cell content. Rejected: leaving it as "it happens to work" precedent — it doesn't generalize, as this page just demonstrated.
**Verification:** Confirmed live in-browser — reloaded the page and every column now reads with clear separation.

### 17. WCAG AA contrast rule doesn't specify which color plays which role — FIXED (2026-07-28)

**Found:** Sprint 4, starting ticket 4 (Brand Settings, API §3.5, UI Design System §10).
**Problem:** API §3.5 requires PATCH to "validate WCAG AA contrast for the color pair before saving," and UI Design System §10 says "every brand-color/neutral-background combination is contrast-checked" — but neither doc says which of `BrandSettings.primaryColor`/`secondaryColor` plays foreground vs. background, against which specific neutral value, or the exact ratio threshold (AA itself has two: 4.5:1 normal text, 3:1 large text/UI components).
**Root cause:** Genuinely undecided in the docs — §10 states the *policy* (AA minimum, checked at save time) without specifying the *mechanics* (which role each color plays, which neutral it's paired with).
**Fix chosen — and why:** Treated `primaryColor`/`secondaryColor` (whichever are set) as button/badge **background** colors with white (`#FFFFFF`) **foreground** text — matching the widget mechanism doc's own framing of `--brand-primary` as the CSS variable used for accent/CTA styling (§1), and the most common real failure mode for a "brand color" field (a light pastel brand color becomes unreadable under white button text). Applied the stricter 4.5:1 (normal-text) threshold uniformly rather than the 3:1 large-text carve-out, since nothing distinguishes button-label size from body text at this layer. Rejected: treating brand colors as foreground text color on a white page background instead — equally defensible, but less consistent with how `--brand-primary` is actually described as being used (accent/CTA backgrounds, not body text color).
**Verification:** `apps/api/color-contrast.spec.ts` (13/13 unit tests — the pure ratio/AA-pass function) plus `apps/api/verify-brand-settings.mjs` (13/13 checks) — a real pale-yellow `primaryColor` (#FFFF66) correctly fails at a 1.06:1 ratio, a real dark navy (#1A1A2E) correctly passes, and both `primaryColor`/`secondaryColor` failing at once are both named in the same `422` response, not just the first.

### 18. Four `BrandSettings` fields are stored but never consumed anywhere — DEFERRED

**Found:** Sprint 4, starting ticket 4 (Brand Settings), while checking which fields `ChatService`/`PromptsService` actually read before building the settings form.
**Problem:** `formalityNote`, `emojiAllowed`, `signOff`, and `secondaryColor` all exist on the `BrandSettings` model and will become admin-editable through this ticket's form — but a full-codebase grep (`apps/api/src`, `packages/prompts`) turns up zero references to any of the four outside the schema itself. An admin can set them and see nothing change for the guest.
**Root cause:** All four were scaffolded into the schema (DB §"Brand & Prompts") ahead of the Sprint 1/3 chat-pipeline and prompt-assembly work ever being told to read them — a forward-looking schema field that never got wired up, not a regression.
**Fix chosen — and why:** Deliberately left unwired. Building the guest-facing behavior these fields imply (persona formality overrides, emoji-in-response gating, a sign-off line in generated replies, a second brand color actually rendered somewhere) is real prompt-assembly/generation work — a chat-pipeline change, not "a surface over data Sprints 1–3 already produce" (Sprint 4's own Definition of Done). The form still exposes all four as editable (so admins can set them now without the fields silently vanishing later) with a plain note that they're not yet reflected in the live guest experience.
**Verification:** N/A — deliberately unbuilt. Revisit whenever prompt-assembly/`bootstrap()` work picks these up.

### 19. Entity ingestion creates duplicate rows on re-run — no dedup-by-name, missing fields silently default to placeholder values — OPEN

**Found:** Sprint 4, live-verifying the Relationship Bundle builder (ticket 5) — searching entities for "Ocean" to pick an anchor for a test bundle returned "Ocean View Suite" **five times** and "Ocean Facial" three times.
**Problem:** `IngestionService.writeEntities` (`apps/api/src/knowledge/ingestion.service.ts:552`) does a plain `tx.roomType.create(...)` (and the equivalent for every other entity type) with no existence check by name — every time `rooms.md`/`spa.md` gets re-ingested, it creates a brand-new duplicate row rather than updating the existing one. Confirmed directly against the live Bellevue data: one real "Ocean View Suite" (`capacity: 2`, correctly populated) plus four junk duplicates (`capacity: 0`, everything else null), and three "Ocean Facial" duplicates the same way. `capacity: int(f.capacity) ?? 0` is the second half of the problem — a genuinely missing/unextracted field silently becomes the number `0` (a plausible-looking real value) instead of staying absent or blocking the row as `NEEDS_REVIEW`, which is why these duplicates don't visually scream "broken" the way a `null` would.
**Root cause:** `rooms.md`/`spa.md` were re-ingested multiple times across Sprint 2/3's own live-verification sessions (`prisma/ingest-bellevue.mjs` is documented elsewhere as "idempotent," but that idempotency claim is about the ingestion **job**/Chunk rows specifically — the structured-entity write path underneath it was never given the same treatment). Nobody had visually browsed aggregate entity-search results before this ticket — every prior verify script only ever checked entities it created itself, never the shape of the whole seeded catalog — so the pollution went unnoticed until the bundle builder's search UI made it visible for the first time.
**Fix chosen — and why:** Not fixing the ingestion pipeline itself in this ticket — that's real Sprint 2 pipeline behavior (dedup-on-re-ingest semantics, and what a genuinely-missing extracted field should do instead of defaulting to `0`), a decision worth making deliberately rather than as a side effect of a Sprint 4 admin-UI ticket. Cleaned up the **data**, not the code: soft-deleted the four junk "Ocean View Suite" and three junk "Ocean Facial" rows (`deletedAt`, via the same `EntitiesService.remove` path the admin UI itself uses — reversible, not a raw destructive `DELETE`), keeping the one correctly-populated row of each. This unblocks the bundle builder's live verification without silently patching over a pipeline gap that deserves its own real fix.
**Verification:** Re-ran the search live in-browser after cleanup — "Ocean" now returns exactly one "Ocean View Suite" and one "Ocean Facial." **Not verified:** whether other entity types/documents have the same duplication (only room-types and spa-treatments were checked, since those were the two names this ticket's testing happened to touch) — worth a full sweep before Sprint 5 content work, not assumed clean elsewhere.

### 20. UX §12's Analytics mockup names topics finer-grained than anything the pipeline actually extracts — FIXED (2026-07-28)

**Found:** Sprint 4, starting ticket 6 (Analytics: topics distribution + Missing Information panel, API §3.6, UX §12).
**Problem:** UX §12's own worked example lists "Guests Ask Most About" as `Spa · Airport Transfer · Check-in · Family Rooms`, and a Missing Information item as `"Pet Policy (12 Low-Confidence answers this week — no indexed content found)"`. None of these — "Airport Transfer," "Check-in," "Family Rooms," "Pet Policy" — are values the classifier can actually produce. The only per-message subject-matter signal anywhere in the schema is `Message.domainTags` (IA §2's fixed 8-value taxonomy: `accommodation | booking | dining | spa | property | local_area | policies | events`), set by the classifier at a much coarser grain than the mockup's examples. There is no finer per-message "topic" or "subject" field anywhere, and nothing extracts one.
**Root cause:** The UX doc's mockup was written aspirationally, describing what a hotel marketing manager would find most useful (specific, actionable topic names), without checking it against what the classifier's actual `domain` output (AI Engine §2, IA §2) is capable of producing. The same gap exists in both halves of this one screen — topics distribution and the gaps panel — since both read the identical signal.
**Fix chosen — and why:** Both endpoints group by the real `domainTags` values, not fabricated finer-grained topics. Building genuine topic-level extraction (e.g., an LLM call to cluster guest questions into "Pet Policy" vs. "Cancellation Policy" within the `policies` domain) is real classifier/AI-behavior work — a new capability, not "a surface over data Sprints 1–3 already produce" (Sprint 4's own Definition of Done). `recommendedAction` strings are a plain per-domain phrase map (`spa` → `"Upload your spa menu"`, matching API §3.6's own example verbatim) rather than anything content-aware, for the same reason. Rejected: inventing a synthetic finer-grained label client-side — that would misrepresent the data as more specific than it actually is, which is worse than an honestly coarse label.
**Separately, a threshold judgment call the docs don't specify:** UX §12's "12 Low-Confidence answers this week" implies (a) a trailing time window and (b) some bar for "repeated" rather than a one-off. Chose a 7-day trailing window (matching the mockup's literal "this week") and a minimum of 2 LOW-confidence turns for a domain to surface as a gap at all — a single low-confidence turn is far more likely to be one ambiguous guest phrasing than a genuine content gap. Both are admin-query-overridable (`from`/`to` params, same convention as `GET .../analytics/daily`) rather than hardcoded, but default to this 7-day/2-count reading.
**Verification:** `apps/api/verify-analytics.mjs` (13/13 checks) — `getTopics` counts conversations, not raw messages (two messages touching the same domain in one conversation add exactly 1, not 2), sorted descending; `getGaps` isolated to a narrow time window to avoid months of historical test-data noise: a single LOW-confidence occurrence correctly stays below the threshold and never surfaces, a second occurrence surfaces it at exactly `count=2`, a HIGH-confidence message never inflates the count, and `spa`'s `recommendedAction` matches API §3.6's own example verbatim. Live-verified in-browser too: the real Bellevue hotel's full conversation history rendered both ranked lists correctly (Policies topping "Guests Ask Most About" at 141 conversations, matching Missing Information's own Policies gap at 52 low-confidence answers), zero console errors, nav link correctly placed.

### 21. `Notification` has no fan-out/broadcast mechanism, and two of its four types imply things this project hasn't built — FIXED

**Found:** Sprint 4, starting ticket 7 (Notifications wired to real events, API §3.7).
**Problem:** Three separate gaps surfaced reading the schema against what "wired to real events" actually requires:
  1. `Notification.recipientId` is a single `User.id` — there's no group/broadcast concept anywhere in the model. Every trigger site (`LeadsService`, `EscalationsService`, `IngestionService`) only ever has a `hotelId` in scope, never a specific user to notify.
  2. `NotificationType.INGESTION_FAILED` is one enum value, but `Document.status` has two "not fully indexed" outcomes — `FAILED` (a genuinely fatal parse/embedding error) and `NEEDS_REVIEW` (chunks still embedded and written; the document is usable, just flagged for admin cleanup). The docs don't say which one — or both — should notify.
  3. `NotificationStatus` includes `PENDING | SENT | FAILED | READ` — `SENT`/`FAILED` only make sense if something actually delivers the notification somewhere (email, push, webhook). Nothing in this project sends anything outbound; `Integration`/notification-delivery is one of the still-stubbed §3.5 rows.
**Root cause:** `Notification`/`NotificationType`/`NotificationStatus` were scaffolded ahead of any real trigger wiring (same pattern as `DailyMetric` before ticket 1, and the four unconsumed `BrandSettings` fields from ticket 4) — the schema anticipated a fuller notification system than anything Sprint 1–3 ever needed to build.
**Fix chosen — and why:** (1) One `Notification` row per `HotelMembership` for the triggering hotel, no role filter — every member gets notified regardless of role, since nothing in any doc says e.g. "MARKETING doesn't need escalation alerts," and inventing that filter now would be guessing at a rule that doesn't exist. `notifyHotelMembers` (`apps/api/src/notifications/notify.ts`) is a plain helper matching `bumpDailyMetric`'s exact shape/calling convention — takes an already-open `tx`, batch-`createMany`s one row per member. (2) `INGESTION_FAILED` fires only on `status === 'FAILED'`, never `NEEDS_REVIEW` — the enum's own name says "failed," and `IngestionService`'s own doc comments already frame `NEEDS_REVIEW` as "not fatal," so treating it as a failure notification would misrepresent genuinely-indexed content as broken. (3) Notifications are created directly as `PENDING` and only ever transition to `READ` (an admin viewing/dismissing them in the portal) — `SENT`/`FAILED` are left for whenever a real outbound delivery channel gets built; building one now would be new backend infrastructure, not "a surface over data Sprints 1–3 already produce" (Sprint 4's own Definition of Done).
**Verification:** `apps/api/verify-notifications.mjs` (16/16 checks, one SKIP on a single-membership hotel) — `submitAnswer` (consented), `createManual`, `EscalationsService.create`, and the `choose()` `contact_me` capture path each notify every `HotelMembership` for the hotel exactly once; `finalizeDocument('FAILED')` notifies, `finalizeDocument('NEEDS_REVIEW')` does not; `NotificationsService.list()` correctly scopes to the calling recipient, filters by status, rejects an invalid status, and cursor-paginates; `markRead()` transitions `PENDING → READ` and 404s on an unknown id. Live-verified in-browser too: `/admin/notifications` renders the real Bellevue hotel's seeded notifications with correct type labels/summaries/timestamps, the Unread/Read status filter correctly partitions PENDING vs READ rows, and clicking "Mark read" fires the real `PATCH .../:id/read` and updates the row in place — zero real console errors (the one console error seen was the already-documented phantom stale-Turbopack-process artifact from the Brand Settings page, unrelated to this ticket — see tickets 5/6's verification notes).

---

### 22. Agency/Super Admin hotel access was silently unresolvable — `admin_hotels_for_user` only ever checked `HotelMembership` — FIXED

**Found:** Sprint 4, starting ticket 8 (`/session`, hotel CRUD, role-gating verified per role, API §3.1).
**Problem:** `DB §3`'s own doc comment on `OrganizationMembership` says it "grants access across every hotel under an org (Agency Admin / Super Admin)" — as opposed to `HotelMembership`, which "grants access to exactly one hotel (Hotel Admin / Marketing / Reservations / Viewer)." But `admin_hotels_for_user()` (migration `5_admin_hotel_resolver`), the only function that resolves which hotels a user can see, joins exclusively through `HotelMembership`. An `AGENCY_ADMIN`/`SUPER_ADMIN` with only an `OrganizationMembership` row (the access model the schema itself describes for those two roles) resolves to zero hotels — `GET /v1/admin/session` would show them no hotels, `HotelScopeGuard` would 403 `NOT_PROVISIONED` on every admin request, and this ticket's own `POST /v1/admin/hotels` (`AGENCY_ADMIN only`) would be unreachable by the only role allowed to call it.
**Root cause:** the resolver function was written when only `HOTEL_ADMIN` existed in any seeded/tested data (Sprint 1's admin shell) — the org-level access path was never actually exercised end to end until this ticket needed it.
**Fix chosen — and why:** extend `admin_hotels_for_user` (migration `9_admin_hotel_org_access`) to `UNION` in hotels reached via `OrganizationMembership` → `Hotel.organizationId`, matching the DB doc's own stated model exactly rather than inventing a different one. Also added `admin_hotel_role_for_user(userId, hotelId)`, a matching `SECURITY DEFINER` function returning the caller's *effective* role for one specific hotel — the direct `HotelMembership.role` if one exists, else the `OrganizationMembership.role` for that hotel's org, else `NULL` — since role-gating (this same ticket) needs exactly one role value per request regardless of which access path granted it. Same least-privilege shape as `resolve_widget_key`/`admin_hotels_for_user` — can never return more than the caller's real grants.
**Verification:** `apps/api/verify-role-gating.mjs` (21/21 checks) — a throwaway `Organization` + `OrganizationMembership(AGENCY_ADMIN)` + `Hotel` with deliberately *no* `HotelMembership` row resolves via `resolveMemberHotels`, and `admin_hotel_role_for_user` correctly reports `AGENCY_ADMIN` for it; a direct `HotelMembership` role still takes precedence when both exist. Live-verified too: a real Supabase-authenticated browser session against `/admin/hotels`, both as the seeded `HOTEL_ADMIN` (list/edit round-trip, `POST` correctly rejected 403 since this account has no `OrganizationMembership`) and confirmed via the same script's Agency Admin fixture server-side.

### 23. API §3.1 names a `timezone` field on `Hotel` PATCH that doesn't exist in the schema — DEFERRED

**Found:** Sprint 4, ticket 8, while scoping hotel-profile PATCH.
**Problem:** API §3.1 describes `PATCH /v1/admin/hotels/:id` as "Hotel profile (name, slug, timezone, etc.)" — but `Hotel` ([DB §3](07-database-design.md)) has exactly `id`, `organizationId`, `name`, `slug`, `createdAt`. No `timezone` column, nor anything else "etc." could plausibly mean.
**Root cause:** same pattern as `BrandSettings`'s four unconsumed fields (findings-log.md #18) and `DailyMetric` before ticket 1 — the API spec was written to the *eventual* shape of a hotel profile screen, ahead of the schema actually carrying every field it describes.
**Fix chosen — and why:** `PATCH /v1/admin/hotels/:id` accepts exactly `name`/`slug` — the fields that exist — and nothing else. Adding a `timezone` column is a schema migration, i.e. a new backend concept, not "a surface over data Sprints 1–3 already produced" (this sprint's own Definition of Done); deferred to whenever a hotel-profile screen actually needs it, not guessed at here.
**Verification:** `apps/api/verify-role-gating.mjs` — `HotelsService.update()` only ever writes `name`/`slug`, confirmed by the Prisma call shape and a round-trip read-back.

### 24. No full role-permission matrix exists beyond two named rules — implementing exactly what's documented, not the rest — DEFERRED

**Found:** Sprint 4, ticket 8, designing role-gating.
**Problem:** Six roles exist ([DB §3](07-database-design.md): `SUPER_ADMIN | AGENCY_ADMIN | HOTEL_ADMIN | MARKETING | RESERVATIONS | VIEWER`), and eight admin surfaces already exist with mutating endpoints (knowledge, entities, relationships, conversations, leads, brand, notifications, and now hotels) — but across every doc, only four concrete role rules are ever stated: this ticket's own backlog line ("VIEWER can't mutate anything, MARKETING can't reassign leads"), `POST /v1/admin/hotels` is "AGENCY_ADMIN only" (API §3.1), and the not-yet-built `/v1/admin/users` is "HOTEL_ADMIN+ only" (API §3.5). Nothing anywhere restricts `RESERVATIONS`, and nothing distinguishes `SUPER_ADMIN` from `AGENCY_ADMIN` at all — not even whether `SUPER_ADMIN` should also pass an "`AGENCY_ADMIN` only" check (the doc uses a bare "only," not the "`HOTEL_ADMIN`**+**" notation it uses one section later for the users endpoint, which reads as a deliberate distinction, not an oversight).
**Root cause:** the roles were scaffolded from PRD §16's one-line list ("Super Admin, Agency Admin, Hotel Admin, Marketing, Reservations, Viewer") with no accompanying permission matrix anywhere in the doc set — the same "named but not fully specified" pattern as `Notification`'s fields (findings-log.md #21) and `BrandSettings`'s unconsumed fields (#18).
**Fix chosen — and why:** implement exactly the four stated rules, nothing invented for the rest. `HotelScopeGuard` blocks any `POST`/`PATCH`/`DELETE` when the caller's resolved role is `VIEWER` (blanket, applies globally to every controller already using this guard — no per-controller changes, no missed endpoint). `LeadsService.update()` rejects setting `assignedOwnerId` when the caller's role is `MARKETING` (`403 LEAD_REASSIGN_FORBIDDEN`) — scoped narrowly to reassignment specifically, since that's the literal wording ("can't reassign leads"), not a blanket lock on every lead mutation. `POST /v1/admin/hotels` checks `role === 'AGENCY_ADMIN'` exactly — not `SUPER_ADMIN` — honoring the doc's literal "only" wording rather than assuming an unstated hierarchy. Every other role × endpoint combination (`RESERVATIONS` on anything; `HOTEL_ADMIN`/`SUPER_ADMIN`/`AGENCY_ADMIN` beyond the two named restrictions) is left fully permitted — inventing further restrictions now would be guessing at rules nobody has decided, exactly what CLAUDE.md's "say so explicitly rather than guessing" instruction is for.
**Verification:** `apps/api/verify-role-gating.mjs` — `VIEWER` rejected on a mutating call and allowed on a read, every other role allowed on the same mutating call; `MARKETING` rejected only when `assignedOwnerId` is present, allowed on every other lead update; `AGENCY_ADMIN` allowed to create a hotel, `HOTEL_ADMIN` rejected.

---

**Next entry number: 25.**
