// @hospitality/ui — the component library from whichever UI Design System option
// gets chosen (docs/08-ui-design-system.md, options A–D).
//
// Deliberately empty right now. Sprint 1's widget renders as plain unstyled HTML
// on purpose (docs/11-development-plan.md: prove the AI pipeline works before
// spending effort on the visual layer) — this package gets built out in Sprint 5,
// once the A/B/C/D decision is made (docs/14-sprint-backlog.md, Sprint 5).
// Building components here now would mean guessing at tokens that don't exist yet.

export {};
